<MudPaper MaxHeight="100%" Class="pa-4" Elevation="0">
    <MudText Typo="Typo.h5" GutterBottom="true">Top Picks</MudText>

    <MudPaper Elevation="0" Class="d-flex overflow-auto my-2 pa-2 gap-4">
        @if (!_templates.Any())
        {
            <MudText GutterBottom="true" Align="Align.Center">There no picks at this moment</MudText>
        }
        @foreach (var template in _templates!)
        {
            <FormCard ChosenTemplate="template" CurrentUser="CurrentUser"/>
        }
    </MudPaper>
</MudPaper>